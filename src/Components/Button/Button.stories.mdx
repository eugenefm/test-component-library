import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { withKnobs, text, select} from '@storybook/addon-knobs'
import Button from './Button';

<Meta title='Button' component={Button} />

# Button
This is the button that I made, isn't it awesome.

## Properties
Here are all the props that it takes, which are automatically defined using `propTypes` and `defaultProps`.

<Props of={Button} />

## Basic usage
Indendation on the code preview looks a little broken in the current version of Storybook - [see this issue](https://github.com/storybookjs/storybook/issues/8078).

<Preview>
	<Story name='Primary Button'>
		<Button 
			aria-label='Call to Action' 
			onClick={action('Button Click')}
		>
			Call to Action
		</Button>
	</Story>
	<Story name='Secondary Button'>
		<Button 
			variant='secondary' 
			aria-label='Call to Action' 
			onClick={action('Button Click')}
		>
			Call to Action
		</Button>
	</Story>
</Preview>

This additional story will be hidden from the docs in `Storyboook 5.3.0`
<Story name='Button with Knobs' parameters={{ docs: { disable: true }}}  decorators={[withKnobs]}>
	<Button 
		variant={select('Variant', {
			primary: 'primary',
			secondary: 'secondary'
		}, 'primary')} 
		aria-label='Call to Action' 
		onClick={action('Button Click')}
	>
		{text('Text','Call to Action')}
	</Button>
</Story>
